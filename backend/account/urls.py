from django.urls import path, include
# from rest_framework_simplejwt.views import TokenObtainPairView, TokenRefreshView, TokenVerifyView
# from rest_framework.routers import DefaultRouter
# from .views import (
#             CustomUserCreate, BlacklistTokenUpdateView,
#             MeView, ProfileEditView,
#             ChangePasswordView, DeleteUserView,
#             UserList, UserDetail,
#             YandexView,
#             VkView,
#     )



urlpatterns = [
    # path('create/', CustomUserCreate.as_view(), name="create_user"),
    # path('yandex/', YandexView.as_view(), name="yandex_create_or_get_user"),
    # path('vk/', VkView.as_view(), name="vk_create_or_get_user"),
    # path('token/blacklist/', BlacklistTokenUpdateView.as_view(), name="token_blacklist"),
    # path('login/', TokenObtainPairView.as_view(), name='token_obtain'),
    # path('refresh/', TokenRefreshView.as_view(), name='token_refresh'),
    # path('me/', MeView.as_view(), name='me'),
    # path('edit-profile/', ProfileEditView.as_view(), name='edit-profile'),
    # path('token/verify/', TokenVerifyView.as_view(), name='token_verify'), 
    # path('change-password/', ChangePasswordView.as_view(), name='change-password'),
    # path('delete-user/', DeleteUserView.as_view(), name='delete_user'),
    # path('users/', UserList.as_view(), name='users'),
    # path('user/<slug:slug>/', UserDetail.as_view(), name='user-detail'),
   

  
   
]